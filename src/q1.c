/****************************************************************************
Student Name: Tim Kelly                       Student Number:  0755233                           
****************************************************************************/

#include "TreeAdt.h"

int main(int argc, char *argv[]){

	if (argc != 2){
		printf("You must enter a single expression to be evaluated (no spaces).");
		return(1);
	}
	else{

		printf( "usage: %s filename", argv[0] );
		printf( "usage: %s filename", argv[1] );
	}


	return (0);
}